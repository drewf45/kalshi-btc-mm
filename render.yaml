services:
  - type: web
    name: kalshi-btc-mm
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    healthCheckPath: /
  - type: web
    name: kalshi-sol-hourly
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_sol_hourly.py
    healthCheckPath: /