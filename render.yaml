services:
  - type: worker
    name: kalshi-relay-shadow
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m relay_engine.shadow_runner
    envVars:
      - key: RUN_MODE
        value: SHADOW
