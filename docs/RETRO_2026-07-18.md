# RETRO — 2026-07-18 (the first backwards read, ordered by WO-P15 FINAL)

## The premise, proven by its own ordering
The logs are always right; the question is only whether we read them. Three
confirmed cases where the past log already said it:

## R-1 · THE HEADLINE — the pack measured FLIP losing before the port called it proven
Drew's 11:17 AM screenshot of the old flipdesk tape:
`R6: trips 5 · WR 0% · avg scratch −4.4 · net/trip −4.4¢ · Wilson-LB −6.4¢`.
The ratchet's own daily line was NEGATIVE at the lower bound; eighteen hours
later the charter called FLIP "live-proven" and ported its knobs as earned.
The MECHANISM was proven (it traded, scratched, narrated); the EDGE was
measured negative — and unread.

**LAW BANKED:** *"proven" must always name WHAT was proven — mechanism or
margin — and a lane's own last pack line is required reading before any
port.* FLIP stays live (R1/R2 stand un-ratified by nothing); its knobs are
now explicitly **UNPROVEN-margin, PROVEN-mechanism**, and its `FLIP R6:` line
renders in every daily pack (ops.daily_pack) so the margin argues for itself
in public, daily.

## R-2 · FLIP's first breath showed the lone-leg deviation 8 hours early
The 00:14 shadow tape: `FLIP posts no@34` on a fresh window — a lone leg
(Ruling 2's exact target) visible in the lane's FIRST proposal, in shadow,
free. It cost 8¢ at 8:01 because the shadow line was read as "sane
side-post" instead of checked against pair doctrine.

**LAW BANKED:** shadow tape is doctrine evidence, not just liveness
evidence — `scripts/tape_grade.py` reads shadow windows too (its queries do
not filter by transport).

## R-3 · The fail-silent class announced itself twice before the law arrived
21:31: missing creds retry-looped politely forever (fail-silent #1 → the
boot-stop law). 00:46: the listener died with "Task exception was never
retrieved" (fail-silent #2 → the supervision law). Same class, two tapes,
two laws written reactively.

**LAW BANKED:** when a failure CLASS repeats, the fix is written for the
CLASS, not the instance. The Adversary's chair owns class-spotting in every
retro, standing.

## Minor notes
- The 102-sum incoherent pages showed their transient-race frequency across
  several windows before the debounce (CHUNK B) landed.
- The depth-starvation storm was visible at 7:58 and caught at 8:06 — the
  fastest signal-to-fix of the day; the §1 law working avant la lettre.

## THE STANDING LAWS (P15 §1, ratified)
1. Every WO ships with its EXPECTED TAPE; `scripts/tape_grade.py` grades the
   deploy inside the daily pack until every line passes twice, then retires.
2. An expected tape unmet within 24h is a FINDING — auto-paged
   (`DEPLOY_GRADE_INCOMPLETE`), never silently forgotten.
3. **The weekly retro law:** every Saturday session opens with a backwards
   read — the week's failure ledger, pages, and packs re-read against
   CURRENT doctrine, hunting for present-but-unread signals. Output: one
   short RETRO note; findings join the lens queue.
