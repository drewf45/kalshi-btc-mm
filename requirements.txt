# FLIPDESK (f_worker) runtime deps — the desk imports only these two.
# The signed Kalshi client needs cryptography; every HTTP call uses requests.
# (websockets / python-dotenv / PyJWT / scipy from the retired engines are not used.)
requests==2.32.5
cryptography==46.0.3
